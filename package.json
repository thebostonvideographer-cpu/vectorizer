{
  "name": "trace-vectorizer",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based JPEG/PNG → SVG vectorizer (VTracer)",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve dist -l 8080",
    "predev": "npm run build",
    "build": "node scripts/build.mjs",
    "start": "npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
